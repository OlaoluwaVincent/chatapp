import React from 'react';
import { Accounts } from './components';

type Props = {};

const App = (props: Props) => {
	return (
		<div className='App'>
			<Accounts />
		</div>
	);
};

export default App;

import React from 'react';
import { Login } from './components';

type Props = {};

const App = (props: Props) => {
	return (
		<div className='App'>
			<Login />
		</div>
	);
};

export default App;

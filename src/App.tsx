import React from 'react';
import { Register } from './components';

type Props = {};

const App = (props: Props) => {
	return (
		<div className='App'>
			<Register />
		</div>
	);
};

export default App;
